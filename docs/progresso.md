# Progresso — Projeto Célula

> Checklist de acompanhamento. Não é um sprint com cerimônia — é um documento vivo, atualizado conforme o trabalho avança, seguindo o mesmo princípio dos demais markdowns do projeto: registra o que já foi decidido/feito, não o que "deveria" acontecer em teoria. Marcar `[x]` quando concluído; adicionar uma linha nova sempre que uma decisão nova entrar em qualquer um dos outros documentos.

---

## Fase 0 — Definição (concluída)

Tudo que precisava ser decidido antes de qualquer código existir.

- [x] Visão e problema central definidos (`projeto-celula.md` §1)
- [x] Escopo por fase (V1 vs V2) fechado (`projeto-celula.md` §3)
- [x] Modelo de acesso e permissão decidido — sem hierarquia (`projeto-celula.md` §2)
- [x] Módulo de pregações especificado — biblioteca, temas por série, navegação (`projeto-celula.md` §4)
- [x] Módulo de quebra-gelos especificado — tipos, fluxo, catálogo inicial (`projeto-celula.md` §5)
- [x] Kit de utilitários compartilhados especificado (`projeto-celula.md` §6)
- [x] Stack técnica decidida — React + Capacitor + Supabase + Vercel (`projeto-celula.md` §7)
- [x] Requisitos de segurança definidos — RLS, rate limiting, sem PII persistida (`projeto-celula.md` §7)
- [x] Estratégia de modularidade para não travar a V2 (`projeto-celula.md` §7)
- [x] Sistema de temas por série documentado, com workflow de criação de tema novo (`estilos-pregacao.md`)
- [x] Schema JSON de conteúdo de pregação definido e calibrado com 4 pregações reais (`geracao-pregacao.md`)
- [x] Regras de precedência de fonte (transcrição vs. anotação; ruído severo) formalizadas (`geracao-pregacao.md`)
- [x] Mock visual do projeto gerado e aprovado (`mock-prompt.md`, `mock-aprovado-v2.html`)
- [x] Sistema de ícones decidido — Lucide via fork próprio (`mock-prompt.md`)
- [x] Mecanismo de acompanhamento de progresso definido (este documento)

---

## Fase 1 — Fundação técnica (não iniciada)

Infraestrutura mínima antes de qualquer feature visível.

- [x] Criar fork do repositório Lucide no GitHub — https://github.com/carlosbrito92/lucide
- [x] Criar repositório do Projeto Célula (código) — https://github.com/carlosbrito92/projeto-celula
- [x] Criar projeto Supabase (auth anônima, banco Postgres) — projeto `projeto-celula`, região `sa-east-1`, ref `tvhywnpctttrmzcyueii`
- [x] Definir e aplicar Row Level Security nas tabelas iniciais (pregações, quebra-gelos) — tabelas `pregacoes`/`quebra_gelos` criadas com RLS habilitado, policy de `select` pública para `anon`/`authenticated`, sem policy de escrita (conteúdo editorial só via service role); migração em `supabase/migrations/`
- [x] Configurar projeto React + Capacitor (scaffold inicial, build Android/iOS/PWA funcionando) — Vite+React+TS, `com.minc.celula`; build Android testado de ponta a ponta em tablet físico via adb; PWA (manifest + service worker) validado via `vite preview`; iOS só escafoldado (build real exige macOS/Xcode)
- [x] Configurar deploy no Vercel — projeto `projeto-celula` linkado e conectado ao GitHub (deploy automático a cada push em `main`); produção em https://projeto-celula.vercel.app, verificado servindo build + manifest PWA + service worker (200)
- [x] Criar documento vivo de projeto para a fase de código (equivalente a um `CLAUDE.md`, conforme prática referenciada do artigo do Akita) — hurdles técnicos documentados aqui, não nos markdowns de conteúdo — ver `CLAUDE.md` na raiz do repo
- [x] `scripts/db-seed.ts` ganhou modo seletivo (2026-08-22) — upsert de um único arquivo (`npm run db:seed -- content/pregacoes/x.json`), sem truncate, via chave natural nova (`arquivo_origem`, `db/migrations/0002_arquivo_origem.sql`). Modo completo (sem argumento) inalterado. Ver `CLAUDE.md` "Decisões de arquitetura (Migração Neon)" e hurdle sobre `db-schema.mjs` não ser idempotente

## Fase 2 — Módulo de Pregações (concluída)

- [x] Modelar tabela(s) Supabase para pregações — já coberto pela migração da Fase 1 (`pregacoes` com colunas indexáveis + `conteudo jsonb`); nenhuma migração nova foi necessária
- [x] Implementar renderização dos tipos de bloco universais (`paragrafo`, `versiculo`, `callout`, `frase_chave`, `lista`) — `src/content/BlockRenderer.tsx`
- [x] Implementar renderização dos `componente_tema` (stage, diagnostico, antidoto, versus, analogia, banho_list, humor) e do `merch_section` — `src/content/ComponenteTemaRenderer.tsx` / `MerchSection.tsx`. Descoberta: `componente_tema` não é código exclusivo por tema — qualquer variante pode aparecer em qualquer pregação (confirmado no conteúdo real: `stage` apareceu numa pregação do Igrejar); um único registro por variante, cores resolvidas via CSS vars do tema ativo
- [x] Implementar sistema de tema por série — `src/themes/registry.ts` (6 temas de `estilos-pregacao.md`, dados completos) + `resolveTema()` + `ThemeScope` (CSS custom properties escopadas por componente, não só por tela — cards da Biblioteca aplicam o tema da própria série)
- [x] Implementar tela de biblioteca (destaque + lista + busca) — `src/screens/pregacoes/Library.tsx`, busca client-side tolerante a acento
- [x] Implementar tela de leitura com índice clicável + FAB — `src/screens/pregacoes/Reading.tsx` + `useIndiceFab.ts` (IntersectionObserver + `scrollIntoView`, nunca `href="#id"`)
- [x] Popular com as pregações já calibradas — os 4 JSONs (`content/pregacoes/`) inseridos via `execute_sql` (RLS bloqueia escrita client-side por design; seed é operação administrativa, não código de app)
- [x] Nova calibração: "A Religião Te Confundiu" (Religião Tóxica cap. 3, Pastora Anaily) — introduziu 2 gaps reais entre schema documentado e código, corrigidos junto com o conteúdo: `componente_tema` variante `contraste` (documentada em `estilos-pregacao.md`, nunca implementada em `ComponenteTemaRenderer.tsx`) e `banner_intro.versiculo_ancora` (documentado em `geracao-pregacao.md`, nunca existiu no tipo `BannerIntro` nem em `Reading.tsx`). Ver `CLAUDE.md` "Hurdles técnicos".

Também entregue nesta fase, fora do checklist original: router mínimo próprio (`src/router/`) no lugar de `react-router-dom` — evita uma cadeia de CVEs do modo framework/RSC da lib, que o app não usa; suíte de testes (Vitest + Testing Library) com 44 testes, agora exigida no CI.

- [x] Anotações pessoais na Leitura (2026-08-22) — primeiro feedback real de usuário (GD, reunião de líderes). Destaque de texto livre + nota pessoal, 100% local (`localStorage`, nunca sincronizado com o banco), resiliente a recalibração de conteúdo (destaque "escorregado" se realinha pelo texto salvo, ou vira órfão visível em vez de sumir/apontar errado). Exportar pregação como PDF paginado (`html2canvas`+`jsPDF`, import dinâmico). `src/screens/pregacoes/anotacoes/` — ver `CLAUDE.md` "Decisões de arquitetura (Anotações pessoais na Leitura)". **Correção 2026-08-22 (mesmo dia)**: primeiro teste real de Carlos achou 2 bugs que jsdom não tinha como pegar (gatilho de seleção por `mouseup`/`touchend` não confiável em touch real + botão de nota sempre visível competindo com o fluxo pretendido; PDF com fundo branco + header duplicado + 88MB) — trocado gatilho pra `selectionchange` debounced, criação/edição de nota centralizada num painel próprio (`ListaAnotacoes.tsx`), fundo/elementos fixos corrigidos no export, PDF agora paginado (A4) + JPEG. **Confirmado por Carlos em teste real (2026-08-22, PR #59 mergeado)** — sem pendência de validação restante.

## Fase 3 — Módulo de Quebra-gelos + Utilitários (concluída)

- [x] Modelar tabela(s) Supabase para quebra-gelos — já coberta pela migração da Fase 1 (`quebra_gelos`, `tipo` com 3 valores incluindo `utilitario` puro); nenhuma migração nova foi necessária
- [x] Implementar os três utilitários como módulos isolados (sorteador nome/palavra, sorteador de papel, contador/cronômetro) — `src/utilitarios/`. Sorteador de atribuição e sorteador de papel compartilham o **fluxo de privacidade sequencial** ("passar o celular": setup → passagem revelar/confirmar/próximo por pessoa → gestão do líder com valores ocultos) especificado em `docs/spec-privacidade-sorteio.md` — decisão que chegou depois do plano original de Fase 3 e substituiu o desenho inicial (que tinha os dois widgets com comportamentos de privacidade diferentes)
- [x] Implementar tela de catálogo de utilitários (`/utilitarios`) + tela standalone por utilitário (`/utilitarios/:id`) — aba 100% funcional, `tipo='utilitario'` na mesma tabela de quebra-gelos
- [x] Implementar tela de catálogo de quebra-gelos (`/quebra-gelos`, com pills Todos/Só leitura/Com sorteio) + tela de detalhe (`/quebra-gelos/:id`) com utilitário(s) embutido(s) inline como overlay — reaproveita os widgets de `/utilitarios` sem duplicar lógica
- [x] Popular com o catálogo já mapeado (9 quebra-gelos do primeiro lote, `docs/projeto-celula.md` §5.3) — regras/jogadores/idade/duração populados para os 9; Artista Impostor também ganhou `dica` (campo novo em `QuebraGeloJogoConteudo`, ver `CLAUDE.md` "Decisões de arquitetura (Fase 3)"), renderizado como callout na tela de Detalhe. Achado ao popular: `SorteioPapel` precisou tratar `papeis` preset como valor inicial editável (não override fixo) — um preset parcial como "Detetive × 1" não bate a soma com o total de participantes sem completar via editor, já que a contagem só é conhecida em tempo real
- [x] Trocar ícones placeholder (emoji) pelos ícones Lucide definitivos, usando a tabela de mapeamento já documentada — vendorizados em `src/icons/lucide/` (ver `CLAUDE.md` "Decisões de arquitetura (Sistema de ícones)")
- [x] Persistir nomes de participantes entre widgets/sessões (`localStorage`, `src/utilitarios/passagemSequencial/nomesStorage.ts`) — melhoria pós-fase por feedback real de célula: mesma lista de nomes redigitada toda vez ao trocar de quebra-gelo ("Encontre o Líder" → sorteio de detetive). `usePassagemSequencial.nomes` (hook compartilhado pelos dois widgets) persiste; resultados de sorteio continuam 100% efêmeros. PR #47, 2026-08-13

## Fase 4 — Polimento V1 (não iniciada)

- [ ] Testes (seguindo a prática de TDD desde o início, não retroativo) para utilitários e renderização de conteúdo
- [ ] Revisão de segurança (RLS, rate limiting) antes de qualquer exposição pública
- [ ] Testar build Capacitor real em dispositivo Android e iOS
- [ ] Convidar um pequeno grupo de líderes de célula para uso real / feedback

## Fase 5 — V2: Multiplayer (em andamento)

- [x] Tecnologia de sincronização escolhida, viabilidade confirmada e implementada — **Playroom Kit** (não mais Supabase Realtime — referência obsoleta desde a migração pra Neon, que não tem equivalente nativo; ver `projeto-celula.md` §7/§10 e `CLAUDE.md` "Decisões de arquitetura (V2 — Quem Sou Eu)")
- [x] Lobby via QR code implementado e testado — `src/multiplayer/quemSouEu/` (`Organizador.tsx`/`Participante.tsx`, UI própria via `skipLobby: true`, QR gerado com `qrcode-generator`). Testado de ponta a ponta contra o backend real do Playroom e em device físico Android (incluindo cenário de re-scan)
- [x] Primeiro mini-jogo — **Quem Sou Eu** (trocado do candidato original Artista Impostor: mecânica de QR code resolve Quem Sou Eu mais diretamente, serve de validação do lobby antes do Artista Impostor, que tem mais partes móveis). Fluxo completo funcionando: organizador escolhe categoria → cria sala → participantes escaneiam e digitam nome → organizador vê ao vivo e inicia → cada participante revela sua palavra sorteada em tela landscape (direção de rotação confirmada em device físico). 3 categorias de conteúdo aprovadas por Carlos (Figuras bíblicas, Personagens de desenho animado, Animais) — `src/multiplayer/quemSouEu/categorias.ts`
- [x] Segundo mini-jogo (Artista Impostor) — implementado em `src/multiplayer/artistaImpostor/` (Organizador escolhe categoria → sala → sorteia impostor + palavra → canvas compartilhado com turno livre → votação digital opcional (híbrida com fallback físico) → resultado agregado sem expor voto individual → "jogar outra rodada"). Canvas sincroniza via polling de `strokes` (mesmo mecanismo do exemplo oficial "Live Canvas" do Playroom Kit; polling via `requestAnimationFrame` desde 2026-08-13, trocado de `setInterval(50ms)` — pausa sozinho em background, economiza bateria em WebView Capacitor, PR #46). **Testado de ponta a ponta em 2026-08-07** — primeiro em dev (2 abas Playwright + 1 device físico Android), depois **em produção real** (app nativo instalado no device conectado, fluxo completo "Biblioteca → Quebra-gelos → Artista Impostor → Jogar pelo Celular" → organizador cria sala → Carlos escaneia o QR com o próprio celular → 2 participantes de teste completam o mínimo de 3 → sorteio, canvas, votação, resultado). Achado e corrigido no processo: `Votacao.module.css`/`Resultado.module.css` sem fallback nas CSS vars de tema — como essas telas rodam do lado do participante (sem `ThemeScope`, diferente do organizador), ficavam com estilo quebrado (fundo branco, fonte serifada do browser) até ganharem `var(--x, #fallback)` como `Participante.module.css` já tinha. **Workflow completo (código → PR → merge → seed → device real) considerado validado por Carlos** — teste com mais pessoas reais fica para depois, mas não bloqueia o que já foi feito
- [x] Integração na navegação real do app — botão "Jogar pelo Celular" leva direto pra `/v2/quem-sou-eu`, não mais só por URL direta (PR #24). Fixes de suporte no mesmo pacote: QR corrigido pra não apontar `localhost` dentro do app nativo (PR #25), `vercel.json` com rewrite SPA corrigindo 404 em rotas profundas num fresh load (PR #26), tela do organizador estilizada com tema do app (PR #27)
- [ ] Pendências abertas de produto: variante "organizador participando", timeout/auto-hide da palavra revelada, edge cases de ordem de operações — ver `projeto-celula.md` §10 "Pendências"
- [x] Terceiro mini-jogo (Spicy) — **Sprint A concluída** (motor de jogo puro, sem UI/Playroom ainda; ver `docs/spicy-spec.md` §7): baralho de 110 cartas com distribuição de §3.1 (`src/multiplayer/spicy/baralho.ts`), resolução de desafio incl. modo "ambos os traços" do Copy Cat (`desafio.ts`), aviso de sequência não-bloqueante (`sequencia.ts`), reducer de turno declarar/passar/desafiar (`turno.ts`), condições de fim de partida e pontuação (`fimDePartida.ts`). Troféu por última carta (jogar a última carta sem ser desafiado, ou desafiado e vencer, dá 1 troféu + mão nova de 6) e World's End implementados nesta sprint — **World's End é toggle de setup** (`OpcoesPartida.worldsEndAtiva`, default `false`), mesmo tratamento da variante Spice It Up!, não peça fixa do motor (decisão de Carlos, 2026-08-10: reembaralhamentos repetidos numa sessão casual sobem a chance dela sair cedo e derrubar a partida sem aviso). 192 testes (TDD), typecheck e lint limpos — PR #34, merged em `main`.
- [x] Terceiro mini-jogo (Spicy) — **Sprint B concluída** (rede + UI mínima, `docs/spicy-spec.md` §7): setup (variante Spice It Up!/World's End/aviso de sequência) → sala com QR → jogo sincronizado via Playroom Kit entre 2+ clients (`src/multiplayer/spicy/`). **Organizador joga junto** (mão + ordem de turno), diferente de Quem Sou Eu/Artista Impostor onde o host só facilita — decisão específica do Spicy por ser jogo de cartas de mesa. Estado sensível (mão de cada jogador, carta real no topo da pilha) sincronizado sob chaves não-descritivas (`s_h4x`/`s_topo`, §2/§6.2). Ações de participante chegam ao host por caixa-postal (`PlayerState.s_acao`), aplicadas via `aplicarAcao` (dispatcher puro novo, testado) e republicadas. Variante Spice It Up! escolhida no setup é só rótulo sincronizado por ora — os 6 efeitos de §5 ficam pra uma sprint própria de motor, ainda não iniciada. UI mínima: texto/botões, sem SVG/visual de carta (isso é Sprint C). **Testado de ponta a ponta com 2 clientes Playwright reais contra o backend Playroom** (sala, entrada, declarar, pilha atualizando no participante, vez passando, desafiar, revelação igual nos dois clientes) — 196 testes, typecheck, lint e build de produção limpos — PR #36, merged em `main`. **Pendente**: renderização visual de carta (Sprint C), integração na navegação real do app (hoje só por URL direta `/v2/spicy`, mesmo estágio que os outros 2 jogos tinham antes de ganhar botão de entrada).
- [x] Terceiro mini-jogo (Spicy) — **Motor das 6 variantes "Spice It Up!" concluído** (§5, `docs/spicy-spec.md`): We Love Chili! (1-3 em Vermelho nunca quebra sequência, mesmo cor estabelecida diferente), Start It Up! (reset pra 1-3 também depois de 8/9, não só 10), Spice Raider (valor 4 marca `pawHolderId`; próxima carta jogada de fato — passar não conta — resolve, Raider fica com a pilha antiga como pontos), Change Your Luck (valor 5, até 2 cartas extras da mão enfiadas embaixo, repostas por compra, imunes a desafio), Turn It Up! (6 e 9 equivalentes no traço valor de um desafio), Copy Cat (nova ação `copiar` — qualquer jogador menos quem declarou por último replica a declaração atual e rouba a vez; desafiar essa jogada força traço 'ambos' automático). Antes (Sprint B) a variante escolhida no setup era só rótulo sincronizado sem efeito; agora `OpcoesPartida.varianteAtiva` chega de verdade no motor (`turno.ts`/`sequencia.ts`/`desafio.ts`) e a UI mínima (`Jogo.tsx`) ganhou os controles específicos de cada variante (picker de cópia, picker de cartas extras, status do Raider, desafio sem traço quando é cópia). 37 testes novos (231 no total) — Copy Cat, Spice Raider e Change Your Luck testados de ponta a ponta com 2 clientes Playwright reais contra o backend Playroom; We Love Chili/Start It Up/Turn It Up cobertos por unit + integração. PR #39, merged em `main`. **Pendente**: renderização visual de carta (Sprint D), integração na navegação real do app.
- [x] Terceiro mini-jogo (Spicy) — **Sprint D concluída** (visual da carta + animação, `docs/spicy-spec.md` §7): `cartaVisual.ts` traduz `Carta` do motor pro schema `shapes[]`/`texts[]` (§6.1/§6.1.1), testado por fidelidade direta ao texto do spec (lê o markdown via `?raw`, não redigita os dados). `Card.tsx` renderiza SVG genérico com as fontes reais do mock (Archivo + JetBrains Mono); `FlippableCard.tsx` adiciona flip 3D + `layoutId` (Framer Motion) pra movimento da carta dentro da própria mão. `Jogo.tsx` trocou os botões de texto da UI mínima por cartas visuais reais. **Decisão de escopo**: sem `layoutId` cross-container mão→pilha entre jogadores diferentes — o `id` da carta codifica cor/valor no texto, sincronizar isso vazaria a identidade antes da hora (contra o sigilo do §2); pilha usa placeholder genérico sempre face-down, revelação pós-desafio mostra a carta real (já pública nesse ponto). `ShuffleAnimation.tsx`/`riffleShuffle.ts` implementam a coreografia de embaralhar decidida na pesquisa (algoritmo de Juha Lindstedt — dividir em 2 pilhas, intercalar de volta), tocada antes de "Iniciar"; puramente decorativa, o embaralhamento real já acontece no motor. 2 bugs achados e corrigidos durante o teste visual antes de reportar como pronto (placeholder da pilha sem cor/valor quebrando a face oculta do flip; cards com altura 0 por receber tamanho via `<div>` wrapper em vez de `className` direto). Aprovado visualmente por Carlos comparando com `docs/Baralho Spicy.dc.html`. 250 testes, typecheck, lint e build de produção limpos — PR #41, merged em `main`. **Pendente**: integração na navegação real do app (hoje só por URL direta `/v2/spicy`) — nenhuma pendência de motor/visual restante, as 4 sprints do Spicy (A/B/C/D) estão concluídas.
- [x] Terceiro mini-jogo (Spicy) — **Reformulação visual da tela de jogo** (`docs/Tela de Jogo Spicy.dc.html`): mesa escura (#12110F) + fio dourado (#C7A15A), mesmo par do verso da carta, substituindo o chrome neutro da Sprint D. Placar de jogadores (contagem de cartas, borda verde em quem está na vez — `contagemMaos`/`jogadores` novos no estado sincronizado, só quantidade, nunca conteúdo), mão em leque com rotação/arco por carta (`fanLayout.ts`, função pura testada), declarar via pills de cor + grade 1-10 (nunca dropdown, `IconeForma.tsx`), cores de ação fixas (Jogar=verde, Desafiar/Errado=vermelho, Copiar=azul, Passar=contorno). Blocos condicionais e telas Setup/Sala/Embaralhando/Participante redesenhados no mesmo sistema — Organizador/Participante saíram do `ThemeScope` genérico do app, paleta própria do Spicy ponta a ponta. Layout paisagem (3 colunas: placar | mesa+declarar | ações) via CSS Grid na mesma árvore de componentes, sem duplicar JSX. 3 simplificações deliberadas vs. o mock, confirmadas com Carlos (documentadas no PR). 256 testes (14 novos), typecheck, lint e build limpos — testado ponta a ponta via Playwright (portrait + paisagem) em cada fase antes de avançar pra próxima, aprovado visualmente por Carlos. PR #43, merged em `main`.
- [x] Terceiro mini-jogo (Spicy) — **Integração na navegação real do app**: entrada própria no catálogo de quebra-gelos (`content/quebra-gelos/spicy.json`, ícone `shuffle`), regras explicando o fluxo 100% digital (sem baralho físico — diferente de Quem Sou Eu/Artista Impostor, que têm modo físico com o app como alternativa) e botão "Jogar pelo Celular" levando pra `/v2/spicy`, mesmo padrão de PR #24. Nenhuma pendência restante do Spicy.
- [x] Terceiro mini-jogo (Spicy) — **Bug de fim de partida corrigido** (`docs/spicy-spec.md` §4, correção 2026-08-13): `fimDePartida.ts`/`turno.ts` encerravam o jogo quando o pote de 3 troféus esgotava, mesmo distribuído a jogadores diferentes — errado (Carlos confirmou: só o **2º troféu do mesmo jogador**, World's End, ou o **monte de compra esgotar** encerram; pote vazio sem 2º troféu de ninguém só significa "sem mais prêmio de troféu", jogo segue). `verificarFimDePartida` trocou `trofeusNoPote<=0` por `pilhaCompraVazia`; `resolverPendenciaUltimaCarta` agora sempre repõe a mão de 6 ao zerar (só pula a concessão do troféu se o pote já esgotou). 6 testes novos/ajustados, 123 testes do módulo Spicy + typecheck + lint limpos.
- [x] Terceiro mini-jogo (Spicy) — **Textos de declaração/desafio com sabor, sorteados em runtime** (`docs/spicy-spec.md` §9): `src/multiplayer/spicy/textos.ts` importa direto `docs/spicy-textos-declaracao-desafio.json` (fonte única, nunca retranscrito), sorteia 1 das 26 variações de declaração / 30 de desafio (`escolherUm` de `utilitarios/shuffle.ts`, reuso — sem duplicar lógica de sorteio) e substitui `[nome-jogador]`/`[nome-jogador-desafiante]`/`[cor+número]` por valores reais. `Jogo.tsx` sorteia uma vez por evento via `useMemo` (chave = declarante+tamanho da pilha para declaração, `ultimoResultado` para desafio — não a cada render) e exibe abaixo do pill "DECLARADO" e no bloco "REVELADO". `ResultadoDesafioPublico` ganhou `declaracaoContestada` (o que foi alegado, capturado em `Organizador.tsx` antes de `aplicarAcao` resolver o desafio) — sem essa carta, o texto de desafio não tinha como saber o que foi contestado. `tsconfig.app.json` ganhou `resolveJsonModule`. 4 testes novos (270 no total), typecheck, lint e build de produção limpos.
- [x] Terceiro mini-jogo (Spicy) — **Bug de reload/reconexão do host corrigido** (P0, reportado em produção): `Organizador.tsx` guardava `EstadoPartida` só em `useState` local, sem nenhum caminho de recuperação — reload ou suspensão do WebView (bloquear/desbloquear tela no app nativo) travava o jogo pra todo mundo, já que só o host processa ações. `insertCoin` agora roda automaticamente no mount (antes só no clique de "Criar sala"), com `reconnectGracePeriod: 120_000`; se `getState('fase')` já for `'jogo'` ao reconectar (mecanismo de sala do Playroom via hash `#r=`/`permId`, independente de `skipLobby`), o app pula setup/sala/embaralhando e reidrata `EstadoPartida` direto de `reidratarEstado`/`turno.ts` — novos campos publicados (`s_monte`, `pilhaCompraQtd`, `pontuacoes`, `maoVaziaAguardandoTrofeu`) mais o que já existia; cartas do meio de `pilhaSpicy` (nunca publicadas, sigilo) reconstruídas com placeholder opaco, já que `turno.ts` só lê `.length`/último elemento delas. `Participante.tsx` pula a etapa de redigitar nome se `myPlayer().getState('nome')` já sobreviveu à reconexão. 7 testes novos de `reidratarEstado` (equivalência com o caminho não-reidratado testada diretamente), typecheck/lint/build limpos. **Pendências de validação em device físico, não fechadas ainda**: reconexão real de host com participantes conectados, tempo real de `reconnectGracePeriod` do SDK, IDs de jogador sobrevivendo entre sessões, participante recarregando em paralelo a ações de outros.
- [x] Terceiro mini-jogo (Spicy) — **Pontuação final, desafios simultâneos, indicador de monte e regra de turno** (P1+P2 da instrução consolidada, `docs/spicy-spec.md`): tela de fim de jogo agora chama `calcularPontuacaoFinal`/`determinarVencedor` (existiam em `fimDePartida.ts` desde a Sprint A, nunca ligados à UI) — placar ordenado por pontuação final, vitória automática de 2 troféus mostrada como "AUTO" em vez do `Infinity` cru. **Desafios simultâneos**: `turno.ts` ganhou `resolverDesafioMultiplo` (`desafiar()` virou wrapper de 1 desafiante em cima dela) — pontos da pilha divididos igualmente entre desafiantes que caem na mesma janela de ~300ms (`Organizador.tsx#receberDesafio`/`resolverJanela`, buffer em `useRef` não `useState` — side effect dentro de updater funcional seria replicado se React reinvocasse), carta extra puxada do monte quando a divisão não fecha exata (exemplo do Carlos: pilha=1, 2 desafiantes → puxa +1, 1 ponto cada), monte insuficiente divide o que der sem travar. Traço usado é o do primeiro desafiante do grupo — spec não definia desempate. **Indicador de monte**: `ProjecaoPublica.pilhaCompraQtd`, pill no header (`Jogo.tsx`). **Regra de turno**: `podeDesafiarAgora` em `turno.ts` (não pode desafiar na própria vez, exceto janela de 5s em partidas de 2 — `EstadoPartida.declaradoEm` + `useJanelaDesafio.ts`, testado com `vi.useFakeTimers` mesmo padrão de `useCountdown.ts`); enforcement real no host (`desafiar()` lança erro), UI só esconde o botão pra não mostrar algo que seria rejeitado. `Acao` não ganhou timestamp — desnecessário, host carimba `Date.now()` no recebimento (mais robusto que confiar no relógio do client). 19 testes novos (151 no total do módulo), typecheck, lint e build de produção limpos. **Pendência de validação em device físico, não fechada**: 2+ desafiantes reais clicando quase ao mesmo tempo (latência real do Playroom vs. janela de 300ms), timer de 5s visual vs. aceite/rejeição real do host no limite exato.
- [x] Terceiro mini-jogo (Spicy) — **UX de carta: clique duplo, animação de voo, fix do flip** (P4 da instrução consolidada): **clique duplo** numa carta numerada da mão (`FlippableCard` ganhou `onDoubleClick`) declara direto o valor/cor real, pulando o painel "Declarar como" — só ativo pra cartas numeradas (wilds não têm um "valor real" óbvio pra declarar sem blefe). **Animação de voo**: `cartaEmVoo` (estado local, nunca sincronizado) some da mão e reaparece num overlay dentro de `.pilhaStack` com o mesmo `layoutId` (`carta.id`) — Framer Motion interpola a posição sozinho (mesma técnica já usada mão↔mão); pilha sincronizada real continua mostrando só o placeholder face-down (Sprint D, sem mudança); `.pilhaStack` deixou de ser condicional a `declaracaoAtual` pra ter onde a carta pousar mesmo jogando a 1ª carta de uma rodada nova (round-trip com o host ainda não voltou nesse instante). **Fix do flip na revelação**: causa raiz confirmada — `revelada` no bloco REVELADO estava hardcoded `true`, nunca mudava de valor num componente já montado, então o Framer Motion nunca via o alvo do `animate` mudar e a troca de carta era instantânea. `BlocoRevelado` extraído com `reveladaVisivel` local (nasce `false`, vira `true` um `requestAnimationFrame` depois) + `key={cartaRevelada.id}` no pai (força remontagem por episódio — sem isso, o 2º desafio reusaria a instância já revelada). Typecheck, lint, build de produção e os 151 testes existentes (não são testes de UI/animação — módulo não tem testing-library configurado pra spicy) permanecem limpos. **Sem verificação em device/browser real nesta sessão** — clique duplo em touch (conflito com tap único), timing real da animação de voo com latência de rede, e o flip repetido em 2 desafios seguidos rápido ainda precisam de teste manual antes de considerar 100% pronto.
- [x] Terceiro mini-jogo (Spicy) — **Teste em device físico do PR consolidado (P0-P4) revelou um bug maior que o Bug 0 original, corrigido no roteador do app inteiro** (não específico do Spicy): testando reload/reconexão do host num tablet Samsung real (build APK debug local), confirmado via `logcat` que o processo do app é derrubado pelo Android em background (`Process com.minc.celula has died: prev LAST` — comportamento normal de LMK sob pressão de memória, não bug do Capacitor) — e ao reabrir, `window.location.pathname` sempre volta pra `/` (o WebView recarrega `https://localhost/` do zero, não preserva o path de `pushState`). Resultado: o usuário nunca voltava sozinho pra tela do Spicy, sempre caía na Biblioteca — o fix de reidratação (P0 desta sessão) nunca chegava a rodar, porque o `Organizador` nem remontava. Corrigido em `src/router/Router.tsx`: `path` inicial passa a checar `localStorage['ultimaRota']` quando `window.location.pathname === '/'` em plataforma nativa (`Capacitor.isNativePlatform()`, nunca em web/PWA — lá um reload real já preserva a URL sozinho), restaurando via `window.history.replaceState`; toda navegação (`navigate()` e `popstate`) persiste a rota atual. **Confirmado funcionando em device físico**: força-parei o app (`am force-stop`, mais destrutivo que o kill natural do SO) estando na tela de setup do Spicy, reabri, e o app foi direto pra tela do Spicy — antes desse fix, sempre caía na Biblioteca. 6 testes novos (`RouterRoutePersistence.test.tsx`, mockando `Capacitor.isNativePlatform`), 300 testes no total do projeto, typecheck, lint e build limpos. **Achados adicionais do teste em device, registrados mas não corrigidos nesta rodada**: (1) reconectar numa sala ainda na fase 'sala' (antes de "Iniciar") funciona mas é redundante — precisa redigitar nome e clicar "Criar sala" de novo (a sala/participantes não se perdem, só a tela local); (2) não foi possível confirmar em device a reidratação funcionando com `fase='jogo'` de fato — só testado via unit test, já que o kill natural do SO não foi reproduzível de forma confiável na janela de teste e o `force-stop` (mais fácil de disparar sob demanda) limpa o hash da URL antes mesmo do roteador entrar em ação; (3) `content/quebra-gelos/spicy.json` (texto de regras exibido no catálogo) ainda descreve a regra antiga e errada de fim de jogo ("acaba quando os 3 troféus do pote se esgotam") — desatualizado desde a correção de fim de jogo mais cedo nesta sessão, conteúdo não código.
- [x] Terceiro mini-jogo (Spicy) — **Bug de lógica corrigido: autodesafio (declarante desafia a própria declaração)**, achado em 2026-08-14 investigando um relato de freeze pós-desafio (freeze não confirmado nesta sessão — ver abaixo). Reproduzido ao vivo contra produção (organizador via Playwright headless + 2 clientes reais, 1 deles ["Celular"] apareceu simultaneamente como `ultimoDeclaranteId` e como um dos `desafiantesIds` do mesmo episódio, resultado "Celular desafiou e venceu" contra a própria mentira). Causa: `podeDesafiarAgora` (`src/multiplayer/spicy/turno.ts:464-469`) só bloqueia desafiar quando `desafianteId === jogadores[indiceDaVez]` (vez atual) — mas `declarar()` já avança `indiceDaVez` pro próximo jogador na mesma ação (`turno.ts:376`), então o próprio declarante deixa de estar "na vez" imediatamente e passa a satisfazer `podeDesafiarAgora`. Falta um segundo guard (`desafianteId !== estado.ultimoDeclaranteId`), mesmo padrão que `copiar()` já tem (`turno.ts:420-422`, `if (jogadorId === estado.ultimoDeclaranteId) throw`). Matemática de cartas/monte do episódio conferida à parte e bate certo com a regra (não é sintoma de outro bug). **Confirmado uma 2ª vez** no mesmo teste: screenshot do device "PC" mostra o botão "Desafiar!" visível pra ele mesmo logo após ele próprio declarar "Vermelho 4" (turno já em "Vez de: Celular") — mesmo mecanismo, reproduzível à vontade, não foi coincidência da 1ª ocorrência. **Freeze original não reproduzido**: 3 clientes reais (organizador headless + 2 devices) jogaram várias rodadas incl. desafio simultâneo de 2 jogadores contra produção (commit `1e343ad`, já com #48/#49) sem erro JS/console nem stall de `requestAnimationFrame` capturados. Hipótese mais forte antes do teste (rajada de ~15 `setState` em `publicarEstado` reentrando via `usePlayersList(true)` do próprio host, `Organizador.tsx:59-103`/`297-338`) não se confirmou nas tentativas feitas — pode precisar de latência/condições específicas não replicadas (device físico do usuário original rodava um APK desatualizado, sem #48/#49, quando o freeze foi visto pela primeira vez; já corrigido/reinstalado nesta sessão). **Corrigido**: `podeDesafiarAgora` (`turno.ts:464-469`) ganhou o guard `desafianteId !== estado.ultimoDeclaranteId`; `Jogo.tsx:194-199` (`podeDesafiar`) espelha o mesmo guard client-side pra esconder o botão. 1 teste novo (`desafioMultiplo.test.ts`), 301 testes do projeto + typecheck + lint limpos.
- [x] Terceiro mini-jogo (Spicy) — **Bug de crash corrigido: tela branca ao vivo em produção (2026-08-15), provável causa raiz do "freeze" original relatado.** Carlos reproduziu ao vivo em 2 jogadores (PC + celular real), capturou o console do Chrome no momento exato: `Uncaught TypeError: Cannot read properties of undefined (reading 'map')`, dentro de um `useMemo` no chunk do Spicy, logo após uma navegação/reconexão de sala. Causa: `Jogo.tsx` tratava `projecao.ultimoResultado` como válido só por não ser `null` — `fraseDesafio` (useMemo) fazia `desafiantesIds.map(nome)` e o render de `BlocoRevelado` fazia `resultado.desafiantesIds.length`/`.map`/`resultado.cartaRevelada.id` sem checar se esses campos existiam de fato. `ultimoResultado` vem sincronizado via Playroom (`useMultiplayerState`/`getState`) — no reconnect/troca de sala, o SDK aparentemente pode devolver um valor truthy mas incompleto por um instante antes do primeiro sync real chegar (não confirmado com certeza absoluta o motivo exato do lado do SDK, mas o formato do objeto na hora do crash batia com isso). Como o app não tem nenhum Error Boundary, essa exceção derrubava a árvore React inteira — e como `body` (`index.css:12`) nunca teve `background` próprio, o resultado era uma tela branca pura, sem nem o tema escuro do app por trás (visualmente idêntico ao "freeze" relatado no dia anterior, quando não foi possível reproduzir). Corrigido com `resultadoValido()` (`Jogo.tsx`, novo type guard exportado) checando `desafiantesIds`/`cartaRevelada`/`pontosPorDesafiante` antes de tratar o resultado como renderizável — usado tanto no `useMemo` quanto no gate que decide mostrar `BlocoRevelado`. 5 testes novos (`resultadoValido.test.ts`, função pura, sem precisar de testing-library), 306 testes do projeto, typecheck limpo, lint limpo (1 warning novo de fast-refresh por exportar uma função de um arquivo de componente — mesma categoria já tolerada em `Router.tsx`). **Não é 100% certo que isso explique o freeze original de 2026-08-14** (o freeze foi visto num device com APK desatualizado, sem essa investigação ainda feita) — mas o mecanismo (crash sem Error Boundary → tela branca) é o mesmo, e a suspeita é forte o suficiente pra registrar aqui como a causa mais provável até prova em contrário.
- [x] Terceiro mini-jogo (Spicy) — **Fix: zoom por duplo toque quebrava o clique duplo de declarar carta no mobile.** `index.html` não desabilitava zoom (`<meta name="viewport">` sem `maximum-scale`/`user-scalable=no`) — em touchscreen, duplo toque sem isso é interpretado como gesto de zoom nativo do WebView/navegador, não como dois cliques, então `onDoubleClick` (`FlippableCard`) nunca disparava no celular, só funcionava no desktop (`dblclick` de mouse é nativo, não depende de gesto). Corrigido acrescentando `maximum-scale=1.0, user-scalable=no` ao viewport — o app já é 100% "estilo aplicativo" (tab bar fixa, sem necessidade de pinch-zoom em nenhuma tela), então desabilitar zoom de página não tem trade-off de acessibilidade relevante aqui. **Pendências de confirmação em device físico, não fechadas nesta sessão**: (1) achado por Carlos junto dos bugs acima — "no APK, Host não consegue usar duplo clique pra declarar carta" — não confirmado ainda se o fix do viewport resolve, precisa reinstalar o APK (que ainda não tinha esse fix) e testar de novo; (2) "no APK é possível desafiar a própria carta jogada, causando crash no outro jogador que está no browser" — pendente de confirmar se é o mesmo autodesafio já corrigido rodando num APK desatualizado (sem o fix), ou um caminho de crash à parte — precisa reinstalar o APK atualizado e testar; (3) "celular (Tyrr6) não conseguia jogar a última carta" — suspeita forte de ser consequência do crash de tela branca acima (uma vez que a árvore React cai, nenhum clique funciona mais), mas não confirmado como bug de lógica à parte.
- [ ] Terceiro mini-jogo (Spicy) — **Decisão de produto pendente, ainda não implementada (Carlos, 2026-08-14)**: a carta Troféu (`TipoCarta.trofeu`, `types.ts:10`) hoje é uma carta física normal que pode ser distribuída na mão de um jogador via compra do monte — visualmente indistinguível pro dono de qualquer outra carta na mão, exceto pelo próprio desenho. Carlos vai introduzir uma nova tela pra tratar o(s) Troféu(éis) como elemento compartilhado, visível a **todos** os jogadores (não escondido na mão de ninguém), que some da mesa quando alguém o ganha — mais fiel ao jogo físico (o pote de troféus fica visível na mesa entre todos, não misturado no baralho de ninguém). Ainda não desenhado/especificado a fundo; mudança de UI (e possivelmente de `EstadoPartida`/`turno.ts`, já que hoje o troféu circula pelo mesmo `maos[]`/`pilhaCompra` de qualquer carta) — não iniciada.

---

## Como manter este documento

- Marcar `[x]` assim que algo for concluído — não esperar o fim da fase inteira.
- Se uma tarefa nova surgir que não estava prevista aqui, adicionar na fase correspondente (não deixar implícita).
- Se uma decisão em qualquer outro markdown do projeto mudar o escopo de uma fase, refletir aqui também — este documento deve sempre bater com o estado real dos outros cinco.
- Fases não têm data fixa nem duração estimada — o ritmo é ditado pelo tempo disponível de Carlos, não por sprint.
- **Este arquivo é a única fonte de verdade de progresso — nunca deve existir uma segunda cópia editada em paralelo fora do repositório.** Quando Carlos planeja algo fora do Claude Code (spec, decisão, roadmap) e traz o resultado pra incorporar, o material referencia a fase/seção correspondente aqui (ex: "ver Fase 5 — V2 Multiplayer") em vez de carregar sua própria lista de status duplicada. Decidido em 2026-08-05 depois de um desalinhamento real: uma cópia externa mantida por Carlos ainda dizia Fase 3 "não iniciada" enquanto este arquivo já reportava concluída.
